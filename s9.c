#include<stdio.h>
int main(){
    int a;
    printf("enter your two number :");
    scanf("%d",&a);
    if (a%2==0)
    {
        printf("the number is even",a);
        
    }
    else{
        printf("the number is ord ");
    }
}